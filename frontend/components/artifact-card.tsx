import Link from "next/link";export function ArtifactCard({artifact}:{artifact:any}){return <Link href={`/artifacts/${artifact.slug}`} className="card group overflow-hidden"><img src={artifact.image_url} alt={artifact.title} className="h-56 w-full object-cover transition duration-500 group-hover:scale-105"/><div className="p-5"><div className="eyebrow">{artifact.is_demo?"DEMO · تجريبي":artifact.category}</div><h3 className="mt-2 font-serif text-2xl font-bold">{artifact.title}</h3><p className="mt-2 text-sm text-[var(--muted)]">{artifact.historical_period}</p></div></Link>}