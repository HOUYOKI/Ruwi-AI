"use client";import {useParams} from "next/navigation";import {useQuery} from "@tanstack/react-query";import {api} from "@/lib/api";export default function Progress(){const {id}=useParams<{id:string}>();const q=useQuery({queryKey:["analysis",id],queryFn:()=>api<any>(`/analyses/${id}`),refetchInterval:3000});return <div className="container py-20"><div className="card mx-auto max-w-2xl p-8"><h1 className="font-serif text-4xl font-bold">Analysis progress</h1><div className="mt-8 h-4 overflow-hidden rounded-full bg-black/10"><div className="h-full bg-[var(--gold)]" style={{width:`${q.data?.progress||0}%`}}/></div><div className="mt-4">{q.data?.stage||"Loading…"}</div>{q.data?.error_message&&<div className="mt-5 rounded-xl border p-4">{q.data.error_message}</div>}</div></div>}