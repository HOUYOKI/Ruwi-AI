"use client";import {useQuery} from "@tanstack/react-query";import {api} from "@/lib/api";export default function Curator(){const q=useQuery({queryKey:["reviews"],queryFn:()=>api<any[]>("/curator/reviews")});return <div className="container py-16"><h1 className="font-serif text-5xl font-bold">Curator Workspace · مساحة القيّم</h1><div className="card mt-8 p-7">{q.error?"Curator authentication required.":q.isLoading?"Loading…":`${q.data?.length||0} pending reviews`}</div></div>}