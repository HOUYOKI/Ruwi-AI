"use client";import {useQuery} from "@tanstack/react-query";import {api} from "@/lib/api";import {ArtifactCard} from "@/components/artifact-card";import {useState} from "react";export default function Explore(){const [search,setSearch]=useState("");const {data,isLoading,error}=useQuery({queryKey:["artifacts",search],queryFn:()=>api<any>(`/artifacts?language=${document.documentElement.lang||"ar"}&search=${encodeURIComponent(search)}`)});return <div className="container py-16"><div className="eyebrow">Explore · استكشف</div><h1 className="mt-3 font-serif text-5xl font-bold">قصص من الذاكرة الثقافية</h1><p className="mt-4 text-[var(--muted)]">جميع العناصر الحالية تجارب تعليمية موضحة وليست نتائج تعرّف آلي على صور المستخدمين.</p><input aria-label="Search artifacts" className="input mt-8 max-w-xl" placeholder="ابحث عن قطعة…" value={search} onChange={e=>setSearch(e.target.value)}/>{isLoading&&<div className="mt-10 grid gap-6 md:grid-cols-3">{[1,2,3].map(x=><div key={x} className="card h-80 animate-pulse"/>)}</div>}{error&&<div className="card mt-10 p-6">تعذر تحميل القطع. تحقق من تشغيل API.</div>}<div className="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{data?.items?.map((a:any)=><ArtifactCard artifact={a} key={a.id}/>)}</div></div>}