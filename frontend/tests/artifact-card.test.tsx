import {render,screen} from "@testing-library/react";import {describe,it,expect} from "vitest";import {ArtifactCard} from "@/components/artifact-card";describe("ArtifactCard",()=>{it("renders artifact title",()=>{render(<ArtifactCard artifact={{slug:"demo",title:"Demo Artifact",image_url:"/demo/pottery.svg",is_demo:true,historical_period:"Unknown"}}/>);expect(screen.getByText("Demo Artifact")).toBeInTheDocument()})})