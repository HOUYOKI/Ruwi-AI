# RAG ingestion

Knowledge source metadata records rights, trust, language, and verification. Documents are validated, extracted, cleaned, chunked, embedded, and ranked. The repository includes a lexical fallback service for local testing and pgvector-ready production infrastructure.
