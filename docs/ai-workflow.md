# AI workflow

Image validation → provider check → structured vision candidates → confidence and uncertainty → RAG retrieval → source ranking → experience planning → grounded generation → curator review → publication. Prompts are stored outside routes.
