# Troubleshooting

If the frontend cannot load data, confirm backend health and `NEXT_PUBLIC_API_URL`. If analysis returns AI_PROVIDER_NOT_CONFIGURED, set a supported provider and key. If database startup fails, remove stale local volumes only when data loss is acceptable.
