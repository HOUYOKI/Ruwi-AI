# API

Interactive OpenAPI is available at `/docs`. APIs are versioned under `/api/v1` and cover health, authentication, profiles, artifacts, uploads, analyses, plans, quizzes, saved experiences, conversations, curator workflow, sources, analytics, users, museums, and audit logs.
