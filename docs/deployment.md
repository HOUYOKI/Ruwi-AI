# Deployment

Use managed PostgreSQL with pgvector, object storage, HTTPS, a rotated JWT secret, restricted CORS, centralized logs, backups, and a configured AI provider. Disable demo accounts for public deployments.
